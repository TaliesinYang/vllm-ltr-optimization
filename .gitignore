# heavy / generated — never commit
RESULTS/
MODEL/
jsonfiles/
*.jsonl
*.tgz
*.tar.gz
*.pt
*.bin
*.safetensors
*.onnx
__pycache__/
*.pyc
.venv/
.DS_Store

# the base fork is cloned separately on the GPU box, not vendored here
vllm-ltr/

# isolated feature workspaces
.worktrees/

# keep run manifests (text) — do NOT ignore runs/*.md
checkpoints_best_predictor/
checkpoints_best_predictor_seed4*

# Evidence exceptions: the paper's claims trace to these, so they ship with the
# repo despite the blanket rules above. Both are compressed to stay small.
!runs/**/workload-block1.jsonl.gz
!runs/order-logs/order-logs-all.tar.gz
