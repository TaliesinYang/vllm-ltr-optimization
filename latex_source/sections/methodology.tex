% Page budget: exactly 1 page; no figures; at least two tables; no bullets.
\clearpage
\section{Methodology}

% [证据注释] Gateway 完整 pin: 888fba9984a34b23340f08e6faf81ace032f3a01
% (表内用短 pin 888fba9984a 防止单栏溢出;完整 SHA 以本注释与 run manifest 为准)

% [作者手写区] 要点: 定义研究问题、ID/OOD/mixed 场景和端到端测量边界；真机 request shape 与 Tier-2 label generation 一致: chat completions, full history, enable_thinking=false, temperature=0, max_tokens=4096。Bib keys: toolace2024, bfcl, toolathlon2025.

\begin{table}[!t]
  \centering
  \caption{Environment provenance by experimental stage.}
  \label{tab:platform}
  \footnotesize
  \begin{tabularx}{\columnwidth}{@{}p{0.19\columnwidth}XX@{}}
    \toprule
    Component & Tier-2 labeling & Serving benchmark \\
    \midrule
    Evidence & T7 \texttt{extracted/environment.json} & Recorded in run manifest on run day \\
    GPU & NVIDIA GeForce RTX 3090 & Rented GPU, 48 GB VRAM; exact SKU to be recorded in the final run manifest \\
    Host/image & Not recorded in labeling manifest & Hengyuan Cloud image; exact OS to be recorded in the final run manifest \\
    Python & 3.11.10 & Recorded in run manifest on run day \\
    vLLM & 0.19.1 & 0.24.0; source rev. \texttt{ee0da84ab9e0}; target/pinned; to be verified in the final run manifest \\
    PyTorch/CUDA & 2.10.0+cu128; CUDA 12.8 & Exact resolved build to be recorded in the final run manifest \\
    Transformers & 5.14.1 & Exact resolved version to be recorded in the final run manifest \\
    Serving model & Qwen/Qwen3.5-9B; rev. \texttt{c20223623576} & Same pinned revision; path and hash to be recorded in the final run manifest \\
    Gateway & Not applicable & VeloxMesh pin \path{888fba9984a} \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[!t]
  \centering
  \caption{Predictor, labels, and training configuration.}
  \label{tab:model-config}
  \footnotesize
  \begin{tabularx}{\columnwidth}{@{}lX@{}}
    \toprule
    Item & Pinned value \\
    \midrule
    Backbone & \texttt{google-bert/bert-base-uncased}; rev. \texttt{86b5e0934494} \\
    Primary input & Current prompt + tool schema \\
    Objective & Pairwise margin ranking; margin 1.0; relative-length filter 0.2 \\
    Input/training & 512 tokens; batch 16; 1 epoch; learning rate $2\times10^{-5}$ \\
    Replications & BERT: 17/42/73; LightGBM: 42 \\
    Label tiers & Tier 1 recorded/source-derived; Tier 2 Qwen3.5-9B replay \\
    Tier-2 sample & 6,000 examples; seed 42; session-preserving 4-length-bucket stratification \\
    Split & 4,000 train / 1,000 validation / 1,000 test \\
    \bottomrule
  \end{tabularx}
\end{table}

% [作者手写区] 要点: 交代 6000 条的 4 桶计数 1265/1560/1769/1406、session boundary preserved、sample SHA-256=ee5a5889ca3d9bbee7790e7a408bd1664a285b6410b4fee54e45786d3eecb709；schema hash 是 canonical JSON 的 SHA-256，必须引用 manifest 而非手算。

% [作者手写区] 要点: 标签质量与 censor 处理；Tier-2 观测到 censor 约 0.05%，普通 tau/MAE 排除 right-censored 行并另报 censor-aware 指标。学习曲线 validation tau-b 在 500/1000/2000/4000 训练池为 0.605171/0.610124/0.637282/0.632051；用于论证饱和趋势，不得写成单调提升。

% [作者手写区] 要点: benchmark 设计参数来自最终 run manifest；工作负载默认 mixed=150 ID+150 OOD、OOD=200，selection seed=42，OOD labeling 仍覆盖完整 800；策略/repeat/负载必须按实际执行结果填写，不得复制旧计划数字。Bib keys: vllm2023.
