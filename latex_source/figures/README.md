# Figure Outputs

Expected final files are `fig1.pdf` through `fig8.pdf` plus `appendix-a-github-latex-source.pdf`.

The LaTeX source shows evidence-labeled fallback boxes while these PDFs are absent. Do not replace measured plots with synthetic placeholder data. Generate report figures through `scripts/plot_final_report_figures.py` after the corresponding source artifacts exist.
